{
  "name": "react-chat-cognito",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "aws-amplify": "^6.0.0",
    "@aws-amplify/ui-react": "^6.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "destroy": "aws cloudformation delete-stack --stack-name rfq-assistant-stack"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}